# Paganini Core Ecosystem — Security Configurations

# Require packages to be at least 3 days old before installing (3 days = 4320 minutes)
minimum-release-age=4320

# Blocks transitive dependencies from using exotic non-npm sources (e.g. GitHub/HTTP URLs)
block-exotic-subdeps=true
